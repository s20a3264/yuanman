<div class="row">
<div class="col-md-8 col-md-offset-2  border mt md">

<h2>會員登入</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, required: false, autofocus: true , class: "form-control" %>
    <%= f.input :password, required: false, class: "form-control" %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "登入", class: "btn-normal" %>
    <%= link_to "註冊", new_user_registration_path, class: "small padding-rl"  %>
  </div>

<% end %>
</div>
</div>