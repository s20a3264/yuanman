<%= form_for([:manager, @setting]) do |f| %>
	<div class="form-group">
  		<%= f.text_area(:about_us, class: "wysihtml5") %>
  		<hr>
		  <%= f.submit "送出", data: { disable_with: "上傳並更新設定中..." }, class: "btn btn-normal" %>
 	</div>
<% end %>
